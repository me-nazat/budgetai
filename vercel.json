{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd app && npm ci",
  "buildCommand": "cd app && npm run build && cd .. && rm -rf .next && ln -s app/.next .next",
  "outputDirectory": "app/.next"
}
